# Secrets
.env
.env.*
!.env.example

# Dependencies / build output (voor als er een JS/TS-toolchain bijkomt)
node_modules/
dist/
build/
*.zip

# Logs & OS-ruis
*.log
.DS_Store

# Lokale Claude Code-instellingen (niet gedeeld)
.claude/settings.local.json
